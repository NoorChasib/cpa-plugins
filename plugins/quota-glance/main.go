// Package main builds the CPA native plugin shared object.
//
// The boundary is a C ABI carrying JSON envelopes with a schema version, not
// Go's plugin package, so ordinary goroutines work normally inside.
package main

func main() {}
