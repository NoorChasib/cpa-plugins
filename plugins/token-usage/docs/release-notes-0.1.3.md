# Token Usage 0.1.3

Build metadata, module imports, and bundled project references use `NoorChasib/cpa-plugins`. Plugin IDs, database paths, configuration, and collection behavior are unchanged.

Install or update Token Usage from the [combined catalog](../../../README.md#get-started). This release targets Linux amd64. The independent release workflow requires Go/race, browser, native CPA, store-install, and archive integrity checks before publishing.
